# Plugin Build Workflow Template
# Copy this file to .github/workflows/build.yml in your plugin repository

name: Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    uses: LumeWeb/workflows/.github/workflows/build-plugin.yml@develop
    secrets: inherit
    # Uncomment and add dependencies if your plugin requires them:
    # with:
    #   additional_dependencies: go.lumeweb.com/portal-plugin-core
    # Uncomment and add replacements if you need Go module replacements:
    #   replacements: github.com/original/module@v1.0.0=github.com/fork/module@v1.0.1
